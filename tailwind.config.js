/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class', // Enable dark mode via the 'dark' class (we'll toggle it in React)
  content: ['./index.html', './src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {}
  },
  plugins: []
}
